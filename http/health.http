@baseUrl = {{remote.baseUrl}}

### Health Check
GET {{baseUrl}}/
Accept: application/json

> {% client.test("Root endpoint responds OK", function() {
    client.assert(response.status === 200);
    client.assert(response.body.message === "Hello World");
}); %}
